/** List of all arguments and aliases. */

export enum ArgumentEnum {
  version = "version",
  name = "name",
  type = "type",
  clean = "clean",
  verbose = "verbose"
}

export enum AliasEnum {
  version = "v",
  name = "n",
  type = "t",
  clean = "c"
}
