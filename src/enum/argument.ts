/** List of all arguments and aliases. */

export enum Argument {
  version = "version",
  name = "name",
  type = "type",
  clean = "clean",
  config = "config"
}

export enum AliasEnum {
  version = "v",
  name = "n",
  type = "t",
  clean = "p",
  config = "c"
}
