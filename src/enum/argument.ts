/**
 * List of all arguments and aliases.
 */

export enum Argument {
  version = "version",
  name = "name",
  config = "config",
  unknown = "unknown"
}

export enum AliasEnum {
  version = "v",
  name = "n",
  config = "c"
}
