/** All kinds of parameter type. */
export enum ParamTypeEnum {
  executor = "executor",
  application = "application",
  program = "program",
  command = "command",
  argument = "argument",
  alias = "alias"
}
