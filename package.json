{
  "name": "repox",
  "version": "1.0.11",
  "description": "Lightweight monorepo for many huge projects.",
  "main": "./bin/repox.js",
  "bin": "./bin/repox.js",
  "author": "Jakub Olejarczyk",
  "license": "ISC",
  "scripts": {
    "remove": "rmdir /s /q dist",
    "build": "tsc && copy package.json dist && copy README.md dist",
    "link": "npm link ./dist",
    "unlink": "npm unlink ./dist && npm uninstall repox -g",
    "test": "jest --silent",
    "up": "npm run test && npm run build && npm run link",
    "down": "npm run unlink && npm run remove"
  },
  "devDependencies": {
    "@types/core-js": "^2.5.5",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.11",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "core-js": "^3.30.1",
    "tsyringe": "^4.7.0"
  }
}
