/**
 * All folders in the workspace.
 */
export enum WorkspaceFolderEnum {
    current = ".",
    projects = "projects",
    apps = "apps",
    libs = "libs",
    tools = "tools",
    dist = "dist"
}
