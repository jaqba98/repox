/**
 * The enum contains all possible actions which we can perform during generate workspace.
 */
export enum WorkspaceActionEnum {
    createFolder = "createFolder",
    createFile = "createFile"
}

// todo: done