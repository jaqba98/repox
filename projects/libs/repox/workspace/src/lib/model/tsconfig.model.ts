import { IRawTSConfig } from "tsc-alias/dist/interfaces";

/**
 * The real model of the tsconfig configuration file on the disc
 * with all required properties.
 */

export interface TsconfigModel extends IRawTSConfig {
}
// todo: refactor
