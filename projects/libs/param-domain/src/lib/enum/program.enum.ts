/**
 * List of all programs.
 */

export enum ProgramEnum {
  default = "",
  generate = "generate",
  build = "build",
  unknown = "unknown"
}

export enum ProgramAliasEnum {
  generate = "g",
  build = "b"
}
// todo: refactor