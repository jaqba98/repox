/**
 * List of all arguments and aliases.
 */

export enum Argument {
  version = "version",
  name = "name",
  config = "config",
  type = "type",
  unknown = "unknown"
}

export enum Alias {
  version = "v",
  name = "n",
  config = "c",
  type = "t"
}
