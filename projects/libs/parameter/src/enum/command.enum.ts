/**
 * List of all commands.
 */

export enum CommandEnum {
  default = "",
  workspace = "workspace",
  project = "project",
  unknown = "unknown"
}

export enum CommandAlias {
  workspace = "w",
  project = "p"
}
