/**
 * The enum contains all self close tags.
 */
export enum HtmlSelfCloseTagEnum {
  doctype = `doctype`,
  area = `area`,
  base = `base`,
  br = `br`,
  col = `col`,
  embed = `embed`,
  hr = `hr`,
  img = `img`,
  input = `input`,
  link = `link`,
  meta = `meta`,
  param = `param`,
  source = `source`,
  track = `track`,
  wbr = `wbr`
}
