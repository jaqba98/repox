/**
 * The enum contains all commands available in the HtmlPro program.
 */

export enum HtmlProCommandEnum {
  default = "default",
  html = "html"
}

export enum HtmlProCommandAliasEnum {
  html = "h"
}
